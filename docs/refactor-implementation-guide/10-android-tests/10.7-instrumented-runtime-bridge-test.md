# 10.7 — `RuntimeBridgeInstrumentedTest.kt`

Place at
`app/src/androidTest/java/org/fcast/android/sender/RuntimeBridgeInstrumentedTest.kt`:

```kotlin
package org.fcast.android.sender

import androidx.test.ext.junit.runners.AndroidJUnit4
import androidx.test.platform.app.InstrumentationRegistry
import kotlinx.coroutines.runBlocking
import org.fcast.android.sender.runtime.BackendKind
import org.fcast.android.sender.runtime.JniRuntimeBridge
import org.junit.Assert.assertNotNull
import org.junit.Test
import org.junit.runner.RunWith

/**
 * Verifies that the real JNI library loads, the bridge is constructable,
 * and a status query round-trips through Rust without crashing.
 *
 * Runs on an emulator (or device) via :app:connectedDebugAndroidTest.
 */
@RunWith(AndroidJUnit4::class)
class RuntimeBridgeInstrumentedTest {

    @Test
    fun jniLibrary_isLoadable() {
        // System.loadLibrary("fcastsender") runs in MainActivity's static block.
        // Here we explicitly trigger it again; the second call is a no-op.
        System.loadLibrary("fcastsender")
    }

    @Test
    fun statusPing_returnsParseableJson_forMigration() = runBlocking {
        val ctx = InstrumentationRegistry.getInstrumentation().targetContext
        val bridge = JniRuntimeBridge(ctx)
        val status = bridge.backendStatus(BackendKind.MIGRATION)
        assertNotNull(status.state)
    }

    @Test
    fun statusPing_returnsParseableJson_forGstpop() = runBlocking {
        val ctx = InstrumentationRegistry.getInstrumentation().targetContext
        val bridge = JniRuntimeBridge(ctx)
        val status = bridge.backendStatus(BackendKind.GSTPOP)
        assertNotNull(status.state)
    }

    @Test
    fun startThenStop_doesNotCrash_forMigration() = runBlocking {
        val ctx = InstrumentationRegistry.getInstrumentation().targetContext
        val bridge = JniRuntimeBridge(ctx)
        val started = bridge.startEmbeddedBackend(BackendKind.MIGRATION, "{}")
        assertNotNull(started.state)
        val stopped = bridge.stopEmbeddedBackend(BackendKind.MIGRATION)
        assertNotNull(stopped.state)
    }
}
```

## Why instrumented?

- `System.loadLibrary("fcastsender")` needs a real Android linker.
- The JNI symbols call into Rust code that uses `JavaVM`. Robolectric
  doesn't provide one.
- Emulator (or device) is the only place to verify the JNI contract
  end-to-end without shipping the APK.

## Test characteristics

- **Speed:** ~3 min cold (emulator boot) + ~200 ms per test.
- **Coverage:** Smoke-level. Verifies JNI is reachable; not specific
  contract details.
- **Flakiness:** Possible — emulator stability is famously fickle. Mitigate
  with `reactivecircus/android-emulator-runner`'s built-in retries.

## What this test does NOT do

- Does not run the gst-pop backend (no remote daemon on the emulator).
  The `BackendKind.GSTPOP` status call returns whatever the Rust shim
  reports for "no daemon" (likely `error`); the test only checks
  parseability.
- Does not exercise the capture pipeline (no screen-capture permission
  flow possible in instrumented tests without UI Automator).

## API level

```kotlin
@RunWith(AndroidJUnit4::class)
```

…runs against the emulator's API level. The `android-emulator-runner`
action launches API 34 (sub-step 10.11). For API 30+ behaviour changes,
add an `@SdkSuppress` annotation:

```kotlin
@SdkSuppress(minSdkVersion = 30)
@Test fun foregroundService_dataSync_typeIsAccepted() { … }
```

## Pre-conditions

- Step 05.4 — `JniRuntimeBridge` exists.
- Step 07.5 — JNI symbols stable.
- Step 09.4 — caching makes the emulator boot tolerable.

## Pre-merge checklist

- [ ] Local: `./gradlew :app:connectedDebugAndroidTest` passes on API 34
       emulator.
- [ ] CI: instrumented job in the matrix passes (sub-step 10.11).
- [ ] No JNI symbol drift since the last green build (sub-step 09.10's
       guard).
