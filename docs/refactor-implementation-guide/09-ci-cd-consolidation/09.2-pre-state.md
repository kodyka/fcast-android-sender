# 09.2 — Pre-state on `main`

Verified files:

| File                                                  | Purpose                                                                                |
|-------------------------------------------------------|----------------------------------------------------------------------------------------|
| `.github/workflows/android-release-apk.yml`           | Debug APK build + upload. **Misnamed:** no release-signing path.                       |
| `.github/workflows/gstpop-smoke.yml`                  | `cargo test backend::gstpop -- --include-ignored --test-threads=1`.                    |
| `.github/workflows/slint-viewer-smoke.yml`            | Slint viewer compilation smoke.                                                        |
| `.github/workflows/ui-lint.yml`                       | `ci/ui-validate.sh --no-build`.                                                        |
| `.gitlab-ci.yml`                                      | `assembleRelease` → `app-release-unsigned.apk` artifact, manual trigger.                |
| `.github/actions/android-ci-setup/`                   | Composite action. Installs SDK + NDK r25c + GStreamer 1.28.0. **No cache.**            |
| `scripts/smoke-gstpop.sh`                             | Docker-based gst-pop smoke harness.                                                    |

## Verified pipeline detail

```bash
rg -n 'cargo test' .github/workflows/
```

```
.github/workflows/gstpop-smoke.yml:101:    # --test-threads=1 required: two ignored tests share process-global atomics
.github/workflows/gstpop-smoke.yml:103:    run: cargo test backend::gstpop -- --include-ignored --test-threads=1
```

Two ignored tests share process-global atomics. Step 07 sub-PR 7.5 notes
the cleanup; it doesn't affect step 09.

## The GitLab pipeline

```bash
rg -n 'when:|assembleRelease|app-release' .gitlab-ci.yml
```

`assembleRelease` produces `app-release-unsigned.apk` and stores it as a
30-day artifact. There is no signing step. Releases today are signed
manually post-CI.

## What is missing

| Thing                              | Where it is today                                | Where it should be             |
|------------------------------------|--------------------------------------------------|--------------------------------|
| Signed release APK                 | Developer's laptop, post-download.               | GitHub Actions release job.    |
| Toolchain caching                  | None.                                            | `actions/cache@v4` on each.    |
| Cargo target caching                | None.                                            | Hash-keyed on `Cargo.lock`.    |
| Symbol-stability guard              | Manual.                                          | CI step in the debug pipeline. |
| GitLab pipeline status              | Production path.                                 | Frozen fallback.               |
