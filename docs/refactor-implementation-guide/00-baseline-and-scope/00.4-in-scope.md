# 00.4 — In scope

These trees and files are touched by at least one step. Anything not listed
here is out of scope; see `00.5-out-of-scope.md`.

## Android shell

```text
app/src/main/java/org/fcast/android/sender/
├── MainActivity.java                       → split + Kotlin (steps 03, 04, 08)
├── ScreenCaptureService.java               → hardening (step 01)
├── GstPopService.java                      → routed via RuntimeBridge (step 05)
├── GstPopServiceBridge.java                → kept as JNI shim (step 05, 07)
├── MigrationRuntimeService.java            → routed via RuntimeBridge (step 05)
├── MigrationRuntimeServiceBridge.java      → kept as JNI shim (step 05, 07)
├── (NEW) FcastApp.kt                       → composition root (step 05)
├── (NEW) AppGraph.kt                       → composition root (step 05)
├── (NEW) CaptureResultBus.java             → replaces LBM (step 02)
├── (NEW) capture/                          → ScreenCaptureCoordinator + CaptureEngine (step 04)
├── (NEW) runtime/                          → RuntimeBridge + JniRuntimeBridge (step 05)
├── (NEW) shell/                            → SenderController + UiState (step 08)
├── (NEW) qr/                               → QrScannerLauncher (step 08)
└── (NEW) data/                             → AndroidSecretStore (step 06)
```

## Manifest

```text
app/src/main/AndroidManifest.xml
    + android:name=".FcastApp" on <application>   (step 05)
    + foregroundServiceType audit               (step 11.5)
```

## Rust crate

```text
src/
├── lib.rs                                  → carved up (step 07)
├── backend/
│   ├── mod.rs                              → BACKEND deprecated → registry (step 05)
│   ├── persistence.rs                      → split secrets (step 06)
│   └── (NEW) registry.rs                   → BackendRegistry trait (step 05)
│   └── (NEW) secret_store.rs               → SecretStore trait (step 06)
├── (NEW) app.rs                            → App composition root (step 05)
├── (NEW) platform/                         → gst_init, PlatformApp, PanelStack (step 07)
├── (NEW) jni_bridge/                       → all Java_… symbols (step 07)
├── (NEW) command/                          → probe + http runner (step 07)
├── (NEW) application/                      → struct Application (step 07)
└── (NEW) android_main.rs                   → entry point body (step 07)
```

## Build / CI

```text
gradle/wrapper/gradle-wrapper.properties    (step 11.4)
gradle/libs.versions.toml                   (steps 11.5, 11.7, 11.9)
app/build.gradle                            (steps 09.6, 11.6, 11.8)
.github/actions/android-ci-setup/           (step 09.3)
.github/workflows/android-release-apk.yml   (renamed → debug; step 09.4)
.github/workflows/android-release-apk.yml   (NEW; step 09.5)
.github/workflows/gstpop-smoke.yml          (step 10.9: drop --test-threads=1)
.gitlab-ci.yml                              (step 09.7: freeze banner)
```

## Tests

```text
app/src/test/java/org/fcast/android/sender/         (step 10)
app/src/androidTest/java/org/fcast/android/sender/  (step 10)
tests/ui_snapshots.rs                                (unchanged; pre-merge gate)
```
