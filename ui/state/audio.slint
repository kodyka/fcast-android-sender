// state/audio.slint — Audio settings.

export global Audio {
    in-out property <int>   source-idx:   0;
    in-out property <bool>  muted:        false;
    in-out property <float> input-gain:   0.7;
    in-out property <int>   bitrate-idx:  1;
}
